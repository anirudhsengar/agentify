{
  "name": "state_lifecycle_plan_build_review_fix",
  "description": "Scout with the State Lifecycle specialist, then run the canonical plan-build-review-fix AIW loop.",
  "tags": [
    "agentify",
    "specialist",
    "state-lifecycle"
  ],
  "inputs": {
    "prompt": {
      "type": "string",
      "description": "The State Lifecycle work request to plan, build, review, and fix."
    },
    "change_type": {
      "type": "string",
      "default": "feature",
      "values": [
        "chore",
        "bug",
        "feature"
      ]
    }
  },
  "parallelism": "sequential",
  "max_runtime_minutes": 120,
  "steps": [
    {
      "id": "scout",
      "description": "Gather State Lifecycle context before implementation.",
      "handler": "subagent",
      "subagent_template": "state-lifecycle",
      "domain": [
        "state-lifecycle"
      ],
      "user_prompt": "Scout the state-lifecycle area for this request: ${inputs.prompt}\nReturn concrete files, invariants, pitfalls, and validation commands.\nRepository validation surface: npm run typecheck; npm test; npm run test:unit; npm run test:maintenance; npm run test:all; npm run test:parity; npm run test:package; npm run test:generated-output; npm run test:security-redteam; npm audit --omit=dev --audit-level=high."
    },
    {
      "id": "implement",
      "description": "Run the canonical AIW after specialist reconnaissance.",
      "handler": "aiw",
      "workflow_type": "plan_build_review_fix",
      "prompt": "${inputs.prompt}\n\nSpecialist scout context:\n${agents[scout].result_text}",
      "change_type": "${inputs.change_type}",
      "depends_on": [
        "scout"
      ]
    }
  ]
}
