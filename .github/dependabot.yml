version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:00"
      timezone: "America/Los_Angeles"
    open-pull-requests-limit: 5
    groups:
      runtime-deps:
        patterns:
          - "@earendil-works/*"
          - "typebox"
          - "jiti"
      dev-deps:
        patterns:
          - "typescript"
          - "@types/*"
          - "tsx"
    # Temporary #60 ceilings keep the automation result inside the approved
    # TypeScript 6 / Node 22 scope while the exact lockfile is generated.
    ignore:
      - dependency-name: "typescript"
        versions: [">=7.0.0"]
      - dependency-name: "@types/node"
        versions: [">=23.0.0"]
      - dependency-name: "tsx"
        versions: [">4.22.4"]
    # agentify treats new runtime deps as gated (see AGENTS.md).
    # Let dependabot bump existing runtime deps, but require explicit
    # maintainer approval for any *new* dependency — surfaced in the
    # PR template.
    labels:
      - "dependencies"
      - "needs-triage"
    commit-message:
      prefix: "deps"
      include: "scope"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "08:00"
      timezone: "America/Los_Angeles"
    open-pull-requests-limit: 3
    labels:
      - "dependencies"
      - "ci"
    commit-message:
      prefix: "ci"
